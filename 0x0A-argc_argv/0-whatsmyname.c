#include <stdio.h>

/**
 * main - program prints name
 * @argc: number of command line arguments
 * @argv: array that contains command line args
 * Return: Always 0
 */
int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
